<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <!-- Фоновое изображение луга -->
    <ImageView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:src="@drawable/meadow_background"
        android:scaleType="centerCrop"
        android:contentDescription="@string/meadow_background_description" />

    <!-- Верхняя панель (Toolbar) для заголовка и иконки настроек -->
    <com.google.android.material.appbar.AppBarLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@android:color/transparent"
        app:elevation="0dp"
        android:fitsSystemWindows="true">

        <com.google.android.material.appbar.MaterialToolbar
            android:id="@+id/topAppBar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            app:title="@string/app_name"
            app:titleTextColor="@android:color/white"
            android:background="@android:color/transparent"/>

    </com.google.android.material.appbar.AppBarLayout>

    <!-- Контейнер для центрирования барана и счетчика -->
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <!-- НОВЫЙ ЭЛЕМЕНТ: Счетчик тапов -->
        <TextView
            android:id="@+id/tapCounterTextView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerHorizontal="true"
            android:layout_above="@id/sheepImageView"
            android:layout_marginBottom="24dp"
            android:text="0"
            android:textSize="48sp"
            android:textColor="@android:color/white"
            android:textStyle="bold"
            android:shadowColor="@android:color/black"
            android:shadowDx="2"
            android:shadowDy="2"
            android:shadowRadius="4"/>

        <!-- Изображение барана, на которое можно нажимать -->
        <ImageView
            android:id="@+id/sheepImageView"
            android:layout_width="250dp"
            android:layout_height="250dp"
            android:layout_centerInParent="true"
            android:src="@drawable/sheep"
            android:contentDescription="@string/sheep_image_description"
            android:clickable="true"
            android:focusable="true"
            android:background="?attr/selectableItemBackgroundBorderless"/>

    </RelativeLayout>

</androidx.coordinatorlayout.widget.CoordinatorLayout>